import { GetAvailableSeatDirective } from './get-available-seat.directive';

describe('GetAvailableSeatDirective', () => {
  it('should create an instance', () => {
    const directive = new GetAvailableSeatDirective();
    expect(directive).toBeTruthy();
  });
});
