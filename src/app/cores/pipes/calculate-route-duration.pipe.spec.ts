import { CalculateRouteDurationPipe } from './calculate-route-duration.pipe';

describe('CalculateRouteDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateRouteDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
