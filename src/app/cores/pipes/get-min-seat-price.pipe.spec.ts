import { GetMinSeatPricePipe } from './get-min-seat-price.pipe';

describe('GetMinSeatPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMinSeatPricePipe();
    expect(pipe).toBeTruthy();
  });
});
