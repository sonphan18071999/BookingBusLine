import { GetAvailableSeatPipe } from './get-available-seat.pipe';

describe('GetAvailableSeatPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAvailableSeatPipe();
    expect(pipe).toBeTruthy();
  });
});
